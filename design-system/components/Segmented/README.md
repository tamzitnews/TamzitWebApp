# Segmented

בחירה אחת מתוך 2–4 אפשרויות קצרות: תדירות (פעם / פעמיים / 3 פעמים), סוג תרומה.

בנוי מ־`fieldset` ו־`input type=radio` אמיתיים. `legend` הוא הכותרת, `options` הן `{value, label, hint}`; `hint` שורה קטנה מתחת (למשל שעות המהדורות). `value`/`onChange` או `defaultValue`.
