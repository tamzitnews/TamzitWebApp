# EditionRow

שורת מהדורה בארכיון: אייקון התקופה, שם, שעה ומספר ידיעות.

`period`, `title`, `meta`, `unread` (נקודה ב־`brand`), `locked` (מהדורה מלפני יותר משבוע אצל משתמש בחינם: "פרימיום" במקום החץ, ולחיצה פותחת את מסך הפרימיום).
