# PhoneFrame

מסגרת טלפון (360×760) להצגת מסכים. רק מתאר, בלי שורת מצב מזויפת.

`caption`, `theme` (לכפות `light`/`dark`), `dir` ו־`lang` (ברירת מחדל `rtl` / `he`; באנגלית ובצרפתית `ltr`). הילדים הם אחד ממסכי המסכים.
