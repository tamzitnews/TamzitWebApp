# IconButton

כפתור אייקון עגול, 44×44. חובה `label` (הופך ל־`aria-label`).

`pressed` למצב דו־מצבי (שמירה), `variant="tint"` על משטח לבן, `variant="hero"` על `surface-hero`. בשורת הפעולות של ידיעה יש בדיוק שלושה: שמירה, שיתוף, משוב.
