# GoodNews

"ונסיים בטוב": הידיעה הטובה שחותמת כל מהדורה, תמיד ובכל רמה ובכל סגנון.

`title` ו־`children`/`body`. על `good-soft`, עם הנבט על עיגול `good`. אחת בלבד, ותמיד אחרונה, לפני `EndOfEdition`.
