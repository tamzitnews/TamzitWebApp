# Switch

מתג הפעלה/כיבוי להגדרה שחלה מיד (התראות, הורדת האזנה). `input type=checkbox role=switch` אמיתי.

`label` ו־`description`, `checked`/`onChange` או `defaultChecked`, `disabled`. מצב שבת אינו מתג: הוא תמיד פעיל ומוצג כשורת מידע.
