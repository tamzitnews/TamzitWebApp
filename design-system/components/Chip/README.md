# Chip

צ'יפ בחירה מרובה (נושאים, סוג משוב, סכום תרומה). כפתור עם `aria-pressed`.

נבחר: רקע `surface-tint`, מסגרת `brand` ו־✓. לא נבחר: מסגרת `line-strong`. `selected` + `onChange` לשליטה מבחוץ, או `defaultSelected`. `icon` מוצג רק כשהצ'יפ לא נבחר (ה־✓ תופס את מקומו).
