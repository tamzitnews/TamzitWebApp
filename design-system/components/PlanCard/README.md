# PlanCard

כרטיס מסלול במסך הפרימיום. `name`, `price`, `features[]`, `highlighted` (מסגרת `brand`), `ribbon` (תווית `sun`), `cta`, `current`.

המחירים הם מקום שמור (`[מחיר]`) עד שייקבעו. כל יתרון בשורה קצרה אחת; בלי השוואה שמקטינה את המסלול החינמי.
