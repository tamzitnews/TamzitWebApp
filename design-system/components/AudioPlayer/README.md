# AudioPlayer

נגן המהדורה הקולית, פס קבוע מעל סרגל הלשוניות. זמין לכל הקוראים.

`title`, `duration` ו־`position` בשניות, `playing`/`onToggle`, `speed`/`onSpeed` (מחזורי: 1, 1.25, 1.5, 0.75). ההשמעה ממשיכה ברקע ובמסך הנעילה. על `surface-hero`; ההתקדמות ב־`sky`.
