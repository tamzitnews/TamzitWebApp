# SquaresMotif

מוטיב שני הריבועים באלכסון, מהאתר: ריבוע `sky` וריבוע `surface-tint`, בלי פינות מעוגלות.

המוטיב יושב בפינה של משטח (למעלה בצד ההתחלה או למטה בצד הסוף), צמוד לקצה, אף פעם לא באמצע ולא מאחורי טקסט. אחד או שניים למסך לכל היותר. `size` בפיקסלים, `flip` מחליף בין הצבעים.
