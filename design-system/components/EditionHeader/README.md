# EditionHeader

ראש המהדורה: יום ותאריך, שם המהדורה, מספר הידיעות וזמן קריאה, וכפתור האזנה.

`period`: `morning` | `noon` | `evening` | `motzash` (קובע שם ואייקון). `date`, `items`, `minutes`, `onListen`; `audio={false}` כשאין מהדורה קולית. זמן הקריאה מחושב, כדי שהקורא יידע מראש כמה זמן זה ייקח.
