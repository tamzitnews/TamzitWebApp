# ShabbatScreen

מה שהאפליקציה מציגה בשבת ובחג, לכל המשתמשים: "שבת שלום", ומתי תגיע מהדורת מוצאי שבת.

`greeting` ("שבת שלום", "חג שמח", "שבת שלום וחג שמח"), `text`, `city`, `time` (צאת השבת לפי העיר). אין מהדורות ואין התראות מהדלקת נרות ועד צאת השבת או החג; מהדורות קודמות נשארות זמינות לקריאה.
