# AppBar

הסרגל העליון: הסמל (או חץ חזרה עם `back`), כותרת, כותרת משנה ופעולות.

`title`, `subtitle`, `back`/`onBack`, `logo={false}`, `actions` (מערך props של `IconButton`), ו־`children` לתג (למשל `SupporterBadge`). בלי צל: הוא יושב על `surface`.
