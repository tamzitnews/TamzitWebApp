import { AppBar, Screen, T } from '@/components/ui';

// PLACEHOLDER — replaced by the owning agent.
export default function EditionTab() {
  return (
    <Screen header={<AppBar title="EditionTab" back />}>
      <T>EditionTab</T>
    </Screen>
  );
}
