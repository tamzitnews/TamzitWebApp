import { AppBar, Screen, T } from '@/components/ui';

// PLACEHOLDER — replaced by the owning agent.
export default function SettingsRhythm() {
  return (
    <Screen header={<AppBar title="SettingsRhythm" back />}>
      <T>SettingsRhythm</T>
    </Screen>
  );
}
