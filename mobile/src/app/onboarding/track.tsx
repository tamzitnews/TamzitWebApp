import { AppBar, Screen, T } from '@/components/ui';

// PLACEHOLDER — replaced by the owning agent.
export default function OnboardingTrack() {
  return (
    <Screen header={<AppBar title="OnboardingTrack" back />}>
      <T>OnboardingTrack</T>
    </Screen>
  );
}
