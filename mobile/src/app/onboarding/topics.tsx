import { AppBar, Screen, T } from '@/components/ui';

// PLACEHOLDER — replaced by the owning agent.
export default function OnboardingTopics() {
  return (
    <Screen header={<AppBar title="OnboardingTopics" back />}>
      <T>OnboardingTopics</T>
    </Screen>
  );
}
